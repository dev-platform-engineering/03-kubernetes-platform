ansible_user: ansible

ansible_become: true
ansible_become_method: sudo

ansible_ssh_private_key_file: ~/.ssh/terraform-bootstrap

ansible_authorized_keys:
  - "{{ lookup('file', 'files/keys/linux-team.pub') }}"
