---
- name: DNS | Install systemd-resolved
  ansible.builtin.apt:
    name: systemd-resolved
    state: present
    update_cache: false

- name: DNS | Configure systemd-resolved
  ansible.builtin.template:
    src: debian_resolved.conf.j2
    dest: /etc/systemd/resolved.conf
    owner: root
    group: root
    mode: "0644"
  notify: Restart systemd-resolved

- name: DNS | Enable and start systemd-resolved
  ansible.builtin.systemd:
    name: systemd-resolved
    enabled: true
    state: started

- name: DNS | Configure /etc/resolv.conf
  ansible.builtin.file:
    src: /run/systemd/resolve/stub-resolv.conf
    dest: /etc/resolv.conf
    state: link
    force: true

- name: DNS | Verify systemd-resolved
  ansible.builtin.command:
    cmd: resolvectl status
  register: resolved_status
  changed_when: false

- name: DNS | Verify repository DNS resolution
  ansible.builtin.command:
    cmd: resolvectl query repo-01.infra.platform.internal
  register: repo_dns_check
  changed_when: false
