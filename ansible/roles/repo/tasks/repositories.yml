---
- name: Configure Rocky Linux repository
  become: true
  ansible.builtin.include_tasks:
    file: rocky.yml
  when: repo_rocky_enabled | bool
# - name: Configure Debian repositories
#   ansible.builtin.include_tasks:
#     file: debian.yml
#   when: repo_debian_enabled | bool
