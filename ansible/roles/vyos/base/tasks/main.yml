---
- name: Ensure the administrative Ansible user exists
  vyos.vyos.vyos_config:
    lines:
      - "set system login user ansible-vyos authentication plaintext-password '{{ vault_vyos_ansible_password }}'"
    match: none

- name: Commit and save base system changes
  vyos.vyos.vyos_config:
    save: true
